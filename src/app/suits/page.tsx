import { SuitsPage } from "@/components/sections/SuitsPage";

export const metadata = {
  title: "Suit Archive — Stark Industries",
  description: "Every Iron Man suit, in chronological order. Explore the legacy of Tony Stark.",
};

export default function Suits() {
  return <SuitsPage />;
}
