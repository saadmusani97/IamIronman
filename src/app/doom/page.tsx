import { DoomReveal } from "@/components/sections/DoomReveal";

export const metadata = {
  title: "Doom — Victor Von Doom",
  description: "Doom is inevitable.",
};

export default function Doom() {
  return <DoomReveal standalone />;
}
